
import React from "react";

const App = () => {
    return (
        <React.Fragment>
            <h1>Example Components</h1>
            <p>More details regarding the components of this project can be seen by visiting the <a href="http://localhost:6006">Storybook instance</a>.</p>
            <p>To get started, run the following command:</p>
            <code>npm run storybook</code>
        </React.Fragment>
    );
};

export default App;